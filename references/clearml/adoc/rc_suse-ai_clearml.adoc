:docinfo:
include::./common_docinfo_vars.adoc[]
include::./rc_suse-ai_clearml-vars.adoc[]
[#art-{article-id}]

// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// SUSE Technical Reference Documentation
// Reference Configuration
// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
//
// DOCUMENT ATTRIBUTES AND VARIABLES
//
// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
// 1. Define variables (document attributes) in the vars file.
// 2. Develop content and reference variables in the adoc file.
// 3. Update the docinfo.xml file as needed.
// 4. Update DC file (at a minimum, deactivate DRAFT mode)
//
// For further guidance, see
//   https://github.com/SUSE/technical-reference-documentation/blob/main/common/templates/start/README.md
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =



= {title}: {subtitle}



== Introduction

// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide a motivation for the document in 2-4 sentences to identify:
//   - what the document is about
//   - why it may be of interest to the reader
//   - what products are being highlighted
// Include an approved SUSE | Partner logo lock-up if relevant/possible
// Include any additional, relevant details
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

This document provides a Technical Reference Configuration for building an enterprise-grade AI compute and machine learning operations (MLOps) platform using *SUSE AI* integrated with *ClearML*. It describes an opinionated but flexible architecture designed to help organizations deploy, operate, and scale machine learning workloads in a *secure, consistent, and observable* manner across on-premises, cloud, and hybrid environments.

As enterprises increasingly adopt artificial intelligence to drive business value, many face challenges in moving from isolated experimentation to reliable, repeatable production deployments. Common obstacles include fragmented tooling, inconsistent environments, limited lifecycle visibility, and difficulties in scaling GPU-enabled workloads while maintaining security and governance. SUSE AI and ClearML together address these challenges by providing a Kubernetes-native foundation for AI workloads combined with end-to-end MLOps capabilities. 

This reference configuration focuses on *platform-level concerns* rather than data science methodologies. It outlines how platform teams can standardize machine learning workflows, manage shared AI infrastructure, and enable multiple data science teams to work efficiently within a governed and observable environment.

Specifically, this document covers:

* A high-level architecture for an enterprise MLOps platform based on SUSE AI and ClearML
* Integration patterns for training, experimentation, pipeline orchestration, and model lifecycle management
* Considerations for security, observability, and operational consistency
* Guidance for scaling AI workloads, including GPU-enabled and cloud-native deployments

The intended audience includes *platform engineers, infrastructure architects, and MLOps teams* responsible for designing and operating a shared AI platform. Installation procedures, hardware sizing, and product-specific configuration details are referenced from official SUSE documentation and are not reproduced in this guide.

The remainder of this document is structured to first describe the *business context and value* of the solution, followed by detailed *technical architecture and operational guidance* to support implementation at enterprise scale.


=== Scope

// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Specify what this guide covers in no more than 2 sentences.
// E.g., "This document ..."
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

This guide covers:

* The *business and technical drivers* for adopting an AI infrastructure management platform
* A *reference architecture* for SUSE AI and ClearML integration
* Core *software and infrastructure components* and their roles
* *Deployment considerations* and validation guidance
* *Security, scalability, and observability* best practices

This guide does not cover:

* Detailed machine learning algorithms or data science workflows
* Application-specific model tuning
* Vendor-specific hardware performance benchmarking
* Step-by-step installation instructions already documented by SUSE or ClearML


=== Audience

// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Identify for whom this document is intended, perhaps with:
//   - topics of interests (e.g., machine learning, security, etc.)
//   - job roles (e.g., developer, administrator, platform architect, etc.)
//   - required skills
// E.g., "The intended audience for this guide includes ..."
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

This guide is intended for professionals involved in designing, deploying, or operating enterprise AI platforms, including:

* Platform engineers
* Infrastructure architects
* DevOps and MLOps engineers
* Cloud-native administrators

To successfully follow this guide, you should have:

* Working knowledge of Kubernetes and containerized workloads
* Experience administering Linux-based systems
* Familiarity with cloud-native concepts
* A basic understanding of the machine learning lifecycle and core MLOps concepts


=== Acknowledgments

// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Acknowledge contributors not listed as authors.
// Something like:
//   The authors wish to acknowledge the contributions of the following
//   individuals:
//   * {contrib1-firstname} {contrib1-surname}, {contrib1-jobtitle}, {contrib1-orgname}
//   * {contrib2-firstname} {contrib2-surname}, {contrib2-jobtitle}, {contrib2-orgname}
// NOTE: If there are none, comment out this section.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

The following individuals contributed to the creation of this document:

* {contrib1-firstname} {contrib1-surname}, {contrib1-jobtitle}, {contrib1-orgname}
* {contrib2-firstname} {contrib2-surname}, {contrib2-jobtitle}, {contrib2-orgname}


== Business aspect
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide high-level statement of the business value and challenges addressed.
// Expand on these in the following sections.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

Enterprises increasingly invest in AI infrastructure to support process automation, AI model development, and management. While many organizations succeed in acquiring powerful hardware, they often struggle to operate it efficiently, securely, and at scale.

Without a centralized infrastructure management layer, AI resources are frequently underutilized, fragmented across teams, and difficult to govern. Inconsistent access controls, limited visibility into usage, and ad hoc workload orchestration lead to poor utilization of expensive compute and increased operational overhead, resulting in lost ROI.

This reference configuration addresses these challenges by providing an enterprise-ready AI infrastructure management platform based on SUSE AI and ClearML. The solution enables organizations to operate shared compute as a controlled, multi-tenant platform while offering essential tools that support AI builders without requiring a separate MLOps stack.


=== Business problem

Organizations attempting to scale machine learning commonly encounter the following challenges:

* *Limited visibility* - Without centralized observability, teams struggle to understand resource allocation, workload placement, and actual compute consumption across the organization.

* *Infrastructure underutilization* - Organizations invest heavily in AI compute but lack centralized control to ensure resources are efficiently shared, scheduled, and fully utilized across teams.

* *Operational complexity* - Running AI workloads across multiple clusters and environments introduces manual processes, fragile configurations, and increased operational risk for platform teams.

* *Security and governance challenges* - Shared AI infrastructure requires consistent enforcement of isolation, access control, and policies to protect sensitive data and intellectual property.

* *Scalability and resource flexibility* - AI workloads demand elastic access to compute resources and require a uniform resource access interface across hybrid environments.

* *Multi-tenancy* - Enterprise platforms must support multiple teams with shared infrastructure while maintaining isolation, resource governance, and access control.

* *Fragmented model development and deployment tooling* - Teams need a consistent toolset to develop, test, and deploy models, but fragmented tools and workflows slow iteration and increase operational friction.

These challenges affect multiple personas, including data scientists, platform teams, IT operations, and business stakeholders responsible for risk and compliance.


=== Business value

This reference configuration addresses these challenges by providing a standardized, enterprise-ready MLOps platform that delivers the following benefits:

* *Accelerated time to value* - Streamlined and repeatable machine learning workflows reduce friction from experimentation to production.

* *Improved operational consistency* - A Kubernetes-native platform ensures consistent deployment and operation of ML workloads across environments, improving infrastructure ROI.

* *Enhanced security and governance* - Integrated security and observability capabilities help protect models, data, and pipelines throughout the lifecycle, suitable for sovereign AI initiatives.

* *Scalable AI infrastructure* - The solution supports growth by enabling elastic scaling of compute resources, including GPU-enabled workloads when required.

* *Future-ready architecture* - A modular and cloud-agnostic design allows organizations to adapt to evolving AI use cases and infrastructure strategies.

By adopting this architecture, you can establish a strong foundation for operationalizing AI while maintaining control, visibility, and security at enterprise scale.


== Architectural overview
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide an architecture diagram that illustrates the solution and its
// core software and compute platform (hardware or cloud) components.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

// Update below the file name and title for architecture diagram (PNG or SVG).
//image::solution-name_architecture-diagram.png[title="Solution name", scaledwidth="90%", align="center"]


This section describes the *reference architecture* for an enterprise AI infrastructure platform built on *SUSE AI integrated with ClearML* The architecture is designed to provide a unified AI infrastructure platform that supports the full lifecycle of AI workloads, from experimentation and training through deployment and monitoring, while enforcing consistency, security, and operational visibility across hybrid and multi-environment setups.

The solution follows a *layered, Kubernetes-native design*. Each layer addresses a specific responsibility, allowing you to operate and scale the platform independently while maintaining a clear separation of concerns. The architecture supports deployment across on-premises, hybrid, and multi-cloud environments, enabling full flexibility.

At a high level, the solution consists of the following layers:

* *Compute and Infrastructure Layer*
* *Container Platform and Management Layer*
* *AI and MLOps Platform Layer*
* *Security and Observability Layer*

An architecture diagram should be included here to illustrate the relationships between these layers and their core components.

image::ClearML+suse.png[title="SUSE AI and ClearML AI Platform Architecture", SUSE AI and ClearML AI Platform Architecture, scaledwidth="100%", align="center"]

*NOTE*: This reference configuration focuses on architectural patterns and integration points. For installation details and hardware-specific guidance, refer to the official SUSE and ClearML documentation.

As illustrated in the figure above, the key components of the solution are:


* *Software*

  ** *ClearML* - Delivers AI infrastructure management capabilities that enable GPU optimization, AI workload orchestration and scheduling, policy-driven resource governance, and full lifecycle control of AI workloads and models.

  ** *SUSE AI* - Provides the enterprise-grade Kubernetes foundation and runtime environment for AI and machine learning workloads.

  ** *SUSE Rancher Prime* - Enables centralized Kubernetes cluster management, access control, and lifecycle operations.

  ** *SUSE Security* - Provides container image scanning, runtime security, and network visibility.

  ** *SUSE Private Registry* - Acts as the secure container image registry for training and inference workloads.

  ** *SUSE Storage* - Provides distributed, cloud-native persistent storage for platform services and AI workloads.

  ** *SUSE Kubernetes Engine (RKE2)* - Provide the secure Kubernetes orchestration layer to run platform services, training jobs, and inference workloads.

  ** *SUSE Linux Enterprise Server* -  Provides the secure, stable, and enterprise-grade operating system foundation for Kubernetes nodes and supporting infrastructure. SLES delivers a hardened Linux environment with long-term support, integrated security updates, and certified compatibility with SUSE AI, RKE2, and GPU drivers, ensuring consistent and reliable operation of AI platform services and workloads.

* *Compute platform*

  ** *Storage and Networking Services* - Support persistent storage, dataset access, artifact retention, and secure network connectivity.
  ** *Underlying Infrastructure (On-Premises, including air-gapped, or Cloud)* - Supplies CPU, memory, optional GPU acceleration, networking, and physical or virtual storage.

Each of these components is described more fully in the sections below.



// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Starting with the software at the top of the solution stack,
// provide detailed descriptions of each component.
// Provide relevant details about the component, including
// role / function, recommended versions, sizings, deployment
// configurations (to be detailed later), etc.
// Use diagrams, as needed, and appropriate links to existing resources
// to provide more context and details.
// Be sure to reference variables defined in the vars file to simplify
// future updates of the document.
//   This variables can include the component name (very useful for long
//   names), supported versions, URLs of its product and documentation
//   websites, etc.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

== Software details

This section describes the primary software components used in the reference architecture and their roles in the infrastructure management and workload management lifecycle.


=== ClearML
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide details about each software component in its own subsection.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

*ClearML* provides the AI infrastructure management and workload orchestration capabilities required to manage the AI training, building, and inferencing lifecycle. It integrates with SUSE AI to deliver infrastructure optimization, resource allocation, experiment tracking, pipeline orchestration, and model governance on top of the Kubernetes platform.

Within this reference configuration, ClearML is responsible for:

* Tracking experiments, pipelines metrics, parameters, and models
* Managing datasets and data lineage
* Built-in multi-tenancy and billing
* Optimizing infrastructure with dynamic fractional GPUs and ClearML’s Unified Memory Technology
* Defining resource quotas, over-quotas, priorities and spillovers

ClearML operates as the primary interface for AI builders, while relying on SUSE AI for secure and scalable workload execution.

=== SUSE AI

*SUSE AI* provides the enterprise-grade foundation for running AI and machine learning workloads on Kubernetes. It delivers a validated, supported platform that enables consistent execution of AI workloads across environments.

In this reference configuration, SUSE AI is responsible for:

* Providing a Kubernetes-native environment optimized for AI workloads
* Enabling consistent deployment of training and inference workloads
* Supporting GPU-enabled workloads where required
* Integrating with container management, security, and observability services

SUSE AI serves as the control plane and execution environment for all machine learning workloads described in this architecture.

=== SUSE Rancher Prime

*SUSE Rancher Prime*  provides centralized management and lifecycle control for the Kubernetes clusters that host the platform and run the workloads.

In this architecture, SUSE Rancher Prime is used to:

* Provision and manage Kubernetes clusters across environments
* Enforce consistent cluster configuration and access control
* Provide centralized visibility into cluster health and resource utilization
* Support multi-cluster and multi-tenant operations

SUSE Rancher Prime enables platform teams to operate infrastructure at scale while maintaining consistency and governance.

=== SUSE Kubernetes Engine (RKE2)

RKE2 provides the secure, enterprise-ready Kubernetes distribution used to run all platform services and machine learning workloads in this architecture. It delivers a CNCF-compliant Kubernetes environment that is hardened by default and suitable for regulated and enterprise environments.

In this reference configuration, RKE2 is responsible for:

* Providing the Kubernetes orchestration layer for SUSE AI and ClearML
* Running platform services, training jobs, and inference workloads
* Enforcing Kubernetes-native scheduling, networking, and resource isolation
* Supporting GPU-enabled workloads where required
* Integrating with SUSE Rancher Prime for centralized lifecycle management

RKE2 forms the foundational orchestration layer for the platform and enables consistent workload execution across on-premises, hybrid, and multi-cloud environments.

=== SUSE Storage

*SUSE Storage*, powered by Longhorn, provides distributed, cloud-native persistent storage for the platform. It delivers highly available and resilient storage that supports stateful platform services and machine learning workloads running on Kubernetes.

In this reference configuration, SUSE Storage (Longhorn) is responsible for:

* Storing ClearML metadata, artifacts, and logs
* Providing persistent volumes for platform and workloads
* Supporting data replication and high availability
* Enabling snapshotting and backup of critical platform data

SUSE Storage is deployed on RKE2 clusters and backed by local or network-attached disks. It provides consistent, Kubernetes-native storage that supports performance, durability, and operational requirements across environments.

=== SUSE Security

*SUSE Security*, powered by NeuVector, provides container and runtime security for the MLOps platform.

In this architecture, SUSE Security is used to:

* Perform vulnerability scanning of container images
* Enforce runtime security policies for AI workloads
* Monitor network traffic between platform components
* Provide real-time threat detection and response

SUSE Security ensures that machine learning workloads and supporting services operate within enterprise security and compliance requirements.

=== SUSE Private Registry

*SUSE Private Registry* provides a secure container image registry for storing and distributing container images used by the platform.

In this reference configuration, SUSE Private Registry is used to:

* Store base images for training and inference workloads
* Manage versioned application and model images
* Integrate with vulnerability scanning and access controls
* Support secure image promotion across environments

Using a centralized registry helps ensure consistency and traceability across development, testing, and production workflows.

=== SUSE Linux Enterprise Server 

*SUSE Linux Enterprise Server (SLES)* provides the secure, stable, and enterprise-grade operating system foundation for the platform. It serves as the base operating system for Kubernetes control plane and worker nodes, as well as supporting infrastructure components used by SUSE AI and ClearML.

In this reference configuration, SLES is responsible for:

* Providing a hardened and supported Linux operating system for Kubernetes nodes
* Ensuring compatibility with RKE2, container runtimes, and GPU drivers
* Delivering long-term stability through enterprise lifecycle management and security updates
* Supporting high-performance compute workloads, including GPU-accelerated training and inference
* Enabling consistent operating system configuration across on-premises, cloud, and hybrid environments

SLES underpins the reliability, security, and operational consistency of the entire AI platform, ensuring that Kubernetes, SUSE AI, and ClearML services run on a certified and enterprise-supported operating system foundation.


=== Compute platform details
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide details about each compute component in its own subsection.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

Need to work on this.


== Deployment 
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide detailed steps the reader should follow to set up an
// environment described in this guide.
// - Link to other resources (such as the SUSE Rancher documentation)
//   to provide context and details rather than duplicating everything
//   in this document.
// - Ordered lists to organize and present procedural steps.
// - Use code blocks for source code and console commands.
//   Commands:
//     [source, console]
//     ----
//     systemctl enable multipathd
//     ----
//   Source code:
//     [source, yaml]
//     ----
//     network:
//       version: 1
//       config:
//         - type: physical
//           name: eth0
//           subnets:
//         - type: dhcp
//     ----
//   - Always include example command output in a listing box.
//      [listing]
//      ----
//      Type   | # |     | Cleanup | Description           | Userdata
//      -------+---+ ... +---------+-----------------------+--------------
//      single | 0 |     |         | current               |
//      single | 1 |     | number  | first root filesystem |
//      ----
//   - Include relevant screenshots with annotations as needed for clarity.
//   - Use admonitions (notes, tips, warnings, etc.) to provide additional
//     context, exceptions, etc.
//
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =


This section provides a high-level overview of the deployment architecture and sequencing for the SUSE and ClearML reference configuration. It describes the major infrastructure and software layers that comprise the solution and explains how these layers are deployed together to form a functional platform for AI workloads.

For deployment purposes, the solution is represented as a *layered stack*, where each layer depends on the services provided by the layer below it. Deployment is performed in a *bottom-up sequence*, ensuring that each foundational capability is in place before higher-level services are introduced.

image::suse-clearml-layer-diagram.png[title="Layered Diagram", SUSE AI and ClearML AI Layered Diagram, scaledwidth="100%", align="center"]


// Provide a representation of the solution as a layered deployment stack.
// See https://documentation.suse.com/trd/hpe/html/kubernetes_rc_rancher-k3s-sles_hpe-proliant-synergy/id-deployment.html#id-deployment-overview
//image::xyz.png[title="Solution name: Deployment", scaledwidth="90%", align="center"]




=== Compute platform

// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide step-by-step guidance for preparing and deploying the
// compute platform.
// Use subsections (level three, ====) for layer components, if needed.
// For example, you could use a ‘ABC Server deployment’ subsection
// and a separate ‘XYZ Storage deployment’ subsection.
// If you do use subsections, be sure to detail Preparation,
// Process, and Considerations in each.
// Remember to use the defined variables for product names,
// websites, etc.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

Leveraging the enterprise grade functionality of the operating system mentioned in the previous section, many compute platforms can be the foundation of the deployment:

* Virtual machines on supported hypervisors or hosted on cloud service providers
* Physical, bare metal or single-board computers, either on-premises or hosted by cloud service providers

[NOTE]
====
To complete self-testing of hardware with {suse-yes-certification-process}[SUSE YES Certified Process], you can download and install the respective SUSE operating system support-pack version of SUSE Linux Enterprise Server and the YES test suite. Then run the tests per the instructions in the test kit, fixing any problems encountered and when corrected, re-run all tests to obtain clean test results. Submit the test results into the SUSE Bulletin System (SBS) for audit, review and validation.
====

[TIP]
====
Certified systems and hypervisors can be verified from {suse-yes-certified-bulletins}[SUSE YES Certified Bulletins] and then can be leveraged as supported nodes for this deployment, as long as the certification refers to the respective version of the underlying SUSE operating system required.
====

// *Preparation*
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide step-by-step guidance for preparing the compute platform component.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

// Add content here



// *Process*
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide step-by-step guidance for preparing the compute platform component.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

// Add content here


// *Considerations*
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Note any considerations for deployment not covered above, such as automation, storage expansion, future scaling, etc.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

// Add content here

=== SUSE Linux Enterprise Server

As the base software layer, use an enterprise-grade operating system, {sles-website}[SUSE Linux Enterprise Server].

*Preparation*

Ensure these services are in place and configured on each node to use:

* Domain Name Service (DNS) - an external network-accessible service to map IP Addresses to host names
* Network Time Protocol (NTP) - an external network-accessible service to obtain and synchronize system times to aid in time stamp consistency
* Software Update Service - access to a network-based repository for software update packages. This can be accessed directly from each node through one of the following:

  ** the {scc-website}[SUSE Customer Center] (SCC)
  ** the organization’s {suse-manager}[SUSE Manager] infrastructure
  ** a local server running an instance of {suse-repository-management-tool}[Repository Mirroring Tool] (RMT)

  +
  [NOTE]
  ===
  During the node’s installation, it can be pointed to the respective update service. This can also be accomplished post-installation with the command line tool, {suse-connect}[SUSEConnect].
  ===

*Deployment process*

On the compute platform node, install the noted SUSE operating system, by following these steps:

** Download the {sles-download}[SUSE Linux Enterprise Server] product (either for the ISO or Virtual Machine image).
** Be sure to identify the appropriate, supported version of SUSE Linux Enterprise Server by reviewing the {rancher-support-matrix}[SUSE Rancher support matrix].
** The installation process is described and can be performed with default values by following steps from the product documentation, see {sles-docs}[Installation Quick Start].

[TIP]
Adjust both the password and the local network addressing setup to comply with local environment guidelines and requirements.

*Deployment considerations*

To further optimize deployment factors, leverage the following practices:

** Automation
*** To reduce user intervention, unattended deployments of SUSE Linux Enterprise Server can be automated
**** for ISO-based installations, by referring to the {sles-agma-installtion-guide}[Agama Installation Guide]



=== SUSE Kubernetes Engine (RKE2)

*Preparation*

1. Identify the appropriate, desired version of the Rancher Kubernetes Engine binary (for example vX.Y.Z) that includes the needed Kubernetes version by reviewing

** the "Supported Rancher Kubernetes Engine Versions" associated with the respective {rancher-support-matrix}[SUSE Rancher version] from "Rancher Kubernetes Engine Downstream Clusters" section, or
** the "Releases" on the {rancher-release}[Download] Web page.

2. On the target node with a default installation of SUSE Linux Enterprise Server operating system, log in to the node either as root or as a user with sudo privileges and enable the required container runtime engine
  
  +
  [source,bash]
  ----
  sudo SUSEConnect -p sle-module-containers/15.3/x86_64
  sudo zypper refresh ; zypper install docker
  sudo systemctl enable --now docker.service
  ----

  Then validate the container runtime engine is working

  +
  [source,bash]
  ---
  sudo systemctl status docker.service
  sudo docker ps --all
  ---

3. For the underlying operating system firewall service, either

** enable and configure the necessary inbound {rancher-ports}[ports] or
** stop and completely disable the firewall service.


*Deployment Process*

The primary steps for deploying this RKE2 are:

[NOTE]
Installing Rancher Kubernetes Engine requires a client system (i.e. admin workstation) that has been configured with kubectl.

1. Download the Rancher Kubernetes Engine binary according to the instructions on product {rancher-docs}[documentation] page, then follow the directions on that page, but with the following exceptions:

2. Create the cluster.yml file with the command rke config
+
[NOTE]
See product documentation for {rancher-example-yaml}[example-yamls] and {rancher-example-config}[config-options] for detailed examples and descriptions of the cluster.yml parameters.
+
  ** It is recommended to create a unique SSH key for this Rancher Kubernetes Engine cluster with the command ssh-keygen
  *** Provide the path to that key for the option "Cluster Level SSH Private Key Path"
  ** The option "Number of Hosts" refers to the number of hosts to configure at this time
  *** Additional hosts can be added very easily after Rancher Kubernetes Engine cluster creation
  *** For this implementation it is recommended to configure one or three hosts
  ** Give all hosts the roles of "Control Plane", "Worker", and "etcd"
  ** Answer "n" for the option "Enable PodSecurityPolicy"

3. Update the cluster.yml file before continuing with the step "Deploying Kubernetes with RKE."

4. If a load balancer has been deployed for the Rancher Kubernetes Engine control-plane nodes, update the cluster.yml file before deploying Rancher Kubernetes Engine to include the IP address or FQDN of the load balancer. The appropriate location is under authentication.sans. For example:
+
[source,bash]
----
LB_IP_Host=""

authentication:
  strategy: x509
  sans: ["${LB_IP_Host}"]
----

5. Verify password-less SSH is available from the admin workstation to each of the cluster hosts as the user specified in the cluster.yml file

6. When ready to create the RKE cluster, execute:
+
[source,bash]
----
rke up
----

7. After the rke up command completes, the RKE cluster will continue the Kubernetes installation process.

[TIP]
To monitor the progress of the RKE2 cluster installation:

1. Export the variable KUBECONFIG to the absolute path name of the kube_config_cluster.yml file.
+
[source,bash]
----
export KUBECONFIG=~/rke-cluster/kube_config_cluster.yml
----

2. Run the command:
+
[source,bash]
----
watch -c "kubectl get deployments -A"
----

The cluster deployment is complete when elements of all the deployments show at least ‘1’ as ‘AVAILABLE’.


Use CTRL+C to exit the watch loop after all deployment pods are running.


To address {availability}[availability] and possible {scaling}[scaling] to a multiple node cluster, etcd is enabled instead of using the default SQLite datastore.


*Deployment Consideration*

To ensure a stable, secure, and scalable Rancher Kubernetes Engine (RKE/RKE2) deployment, consider the following aspects:

1. *Availability* - For production deployments, use an odd number of control-plane/etcd nodes (minimum three) to maintain etcd quorum and prevent loss of cluster availability. When deploying multiple control-plane nodes, use an external load balancer to distribute traffic across the Kubernetes API servers.

2. *Networking* - Ensure that all required Kubernetes, etcd, and container runtime ports are open between cluster nodes. Latency and packet loss between nodes—especially etcd members—can negatively impact cluster stability and performance.

3. *etcd Data Protection* - etcd stores all Kubernetes cluster state and configuration. Enable regular etcd snapshots and store backups outside the cluster to support disaster recovery and cluster restoration.

4. *Security* - Restrict SSH access to cluster nodes using key-based authentication only. Use least-privilege access for the SSH user specified in the cluster.yml file. Protect the generated kube_config_cluster.yml file, as it provides administrative access to the cluster.

5. *Scalability* - Additional worker or control-plane nodes can be added to the cluster after initial deployment by updating the cluster.yml file and re-running the rke up command. Plan IP addressing, DNS, and load balancer capacity accordingly.

6. *Certificate Management* - RKE automatically generates and manages Kubernetes certificates. If a load balancer or external endpoint is used, ensure all required IP addresses and FQDNs are included under authentication.sans in the cluster.yml file to avoid certificate validation issues.

7. *Operating System Configuration* - Disable swap and ensure required kernel modules are loaded on all nodes. Apply consistent OS configuration across nodes to avoid unpredictable behavior.

8. *Upgrade Strategy* - Before upgrading Kubernetes or RKE versions, review the supported version matrix and perform upgrades in a test environment. Back up etcd data prior to any upgrade operation.

9. *Logging and Monitoring* - Plan for cluster-level monitoring and logging early in the deployment lifecycle to simplify troubleshooting and operational visibility.


=== SUSE Rancher Prime

*Preparation*

1. For the respective node’s firewall service, either

* enable and configure the necessary inbound ports or
* stop and completely disable the firewall service.

2. Determine the desired SSL configuration for TLS termination
* Rancher-generated TLS certificate NOTE: This is the easiest way of installing SUSE Rancher with self-signed certificates.
* Let’s Encrypt
* Bring your own certificate

3. Obtain a Helm binary matching the respective Kubernetes version for this SUSE Rancher implementation.

[NOTE]
Enable the respective kubeconfig setting for kubectl , K3s - /etc/rancher/k3s/k3s.yml, to be leveraged by helm command.

*Deployment Process*

While logged in to the node, as root or with sudo privileges, install SUSE Rancher:

1. Install cert-manager

* Set the following variable with the desired version of cert-manager
+
[source,bash]
----
CERT_MANAGER_VERSION=""
----

+
[NOTE]
At this time, the most current, supported version of cert-manager is v1.5.1

** Create the cert-manager CRDs and apply the Helm Chart resource manifest
+
[source,bash]
----
kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/${CERT_MANAGER_VERSION}/cert-manager.crds.yaml

# Add the Jetstack Helm repository
helm repo add jetstack https://charts.jetstack.io

# Update your local Helm chart repository cache
helm repo update

# Install the cert-manager Helm chart
helm install cert-manager jetstack/cert-manager \
  --namespace cert-manager \
  --create-namespace \
  --version ${CERT_MANAGER_VERSION}
----


** Check the progress of the installation, looking for all pods to be in running status:
+
[source,bash]
----
kubectl get pods --namespace cert-manager
----

2. Add the SUSE Rancher helm chart repository:
+
[source,bash]
----
helm repo add rancher-stable https://releases.rancher.com/server-charts/stable
----

3. Create a namespace for SUSE Rancher
+
[source,bash]
----
kubectl create namespace cattle-system
----

4. Prepare to use the Helm Chart for SUSE Rancher:

* Set the following variable to the host name of the SUSE Rancher server instance
+
[source,bash]
----
HOSTNAME=""
----

+
[NOTE]
This host name should be resolvable to an IP address of the K3s host, or a load balancer/proxy server that supports this installation of SUSE Rancher.

* Set the following variable to the number of deployed K3s nodes planned to host the SUSE Rancher service
+
[source,bash]
----
REPLICAS=""
----

* Set the following variable to the desired version of SUSE Rancher server instance
+
[source,bash]
----
RANCHER_VERSION=""
----

* Install the SUSE Rancher Helm Chart
+
[source,bash]
----
helm install rancher rancher-stable/rancher \
  --namespace cattle-system \
  --set hostname=${HOSTNAME} \
  --set replicas=${REPLICAS} \
  --version=${RANCHER_VERSION}
----

** Monitor the progress of the installation:
+
[source,bash]
----
kubectl -n cattle-system rollout status deploy/rancher
----

5. (Optional) Create an SSH tunnel to access SUSE Rancher:
+
[NOTE]
This optional step is useful in cases where NAT routers and/or firewalls prevent the client Web browser from reaching the exposed SUSE Rancher server IP address and/or port. This step requires that a Linux host is accessible through SSH from the client system and that the Linux host can reach the exposed SUSE Rancher service. The SUSE Rancher host name should be resolvable to the appropriate IP address by the local workstation.

* Create an SSH tunnel through the Linux host to the IP address of the SUSE Rancher server on the NodePort, as noted in Step 3:
+
[source,bash]
----
ssh -N -D 8080 user@Linux-host
----

* On the local workstation Web browser, change the SOCKS Host settings to "127.0.0.1" and port "8080".
+
[NOTE]
This will route all traffic from this Web browser through the remote Linux host. Be sure to close the tunnel and revert the SOCKS Host settings when you are done.

6. Connect to the SUSE Rancher Web UI:

* On a client system, use a Web browser to connect to the SUSE Rancher service, from HTTPs.
* Provide a new Admin password.
+
[IMPORTANT]
On the second configuration page, ensure the "Rancher Server URL" is set to the host name specified when installing the SUSE Rancher Helm Chart and the port is 443.

*Deployment Consideration*

To further optimize deployment factors, leverage the following practices

1. *Availability* - In instances where a load balancer is used to access a RKE2 cluster, deploying two additional RKE2 cluster nodes, for a total of three, will automatically make SUSE Rancher highly available.
2. *Security* - The basic deployment steps described above are for deploying SUSE Rancher with automatically generated, self-signed security certificates. Other options are to have SUSE Rancher create public certificates from Let’s Encrypt associated with a publicly resolvable host name for the SUSE Rancher server, or to provide preconfigured, private certificates.
3. *Integrity* - This deployment of SUSE Rancher uses the RKE2 etcd key/value store to persist its data and configuration, which offers several advantages. With a multi-node cluster and this resiliency through replication, having to provide highly-available storage is not needed. In addition, backing up the RKE2 etcd store protects the cluster and the installation of SUSE Rancher and permits restoration of a given state.

After this successful deployment of the SUSE Rancher solution, review the {rancher-manager-docs}[product documentation] for details on how downstream Kubernetes clusters can be deployed



=== SUSE Storage

SUSE Storage provides persistent, software-defined storage services for containerized and cloud-native workloads. In this solution stack, SUSE Storage is used to supply highly available, scalable persistent volumes for Kubernetes workloads managed by SUSE Rancher.

*Preparation*

Before deploying SUSE Storage, ensure the following {sstorage-requirements}[prerequisites] are met:

* A running Kubernetes cluster deployed using SUSE Kubernetes Engine (RKE2) and managed by SUSE Rancher
* Nodes designated for storage meet the minimum hardware requirements:

** Sufficient local disks (HDD or SSD) dedicated exclusively to storage use
** Network connectivity with low latency between storage nodes

* Time synchronization (NTP) is enabled and consistent across all nodes

* Firewall rules allow required inbound and outbound traffic for SUSE Storage components

[NOTE]
Storage devices intended for SUSE Storage must not contain existing partitions or file systems.

[TIP]
For production deployments, it is recommended to use three or more storage nodes to achieve quorum and high availability.


*Deployment Process*
The high-level steps for deploying SUSE Storage are as follows:

1. Deploy SUSE Storage through the SUSE Rancher UI:
* Log in to the SUSE Rancher Web UI
* Navigate to Apps 
* Select the supported SUSE Storage application (for example, SUSE Storage powered by Longhorn or Ceph, depending on the solution design)

2. Configure the storage cluster:
* Select the target Kubernetes cluster
* Assign storage roles to appropriate worker nodes
* Specify disk devices to be used for storage
* Configure replication and fault-tolerance settings

3. Install the SUSE Storage application:
* Review and confirm the Helm chart configuration values
* Deploy the application and monitor the installation progress

4. Verify the deployment:
* Ensure all storage-related pods are running
* Confirm that storage pools and volumes are successfully created
* Validate persistent volume provisioning through a test workload

*Deployment Consideration*

To optimize the storage layer, consider the following:

1. *High Availability* - Deploy storage services across multiple nodes and failure domains to ensure resilience against node or disk failures.
2. *Performance* - Select appropriate disk types (SSD vs HDD) and configure replication factors based on workload performance requirements.
3. *Capacity Planning* - Monitor storage utilization and plan for future expansion by adding disks or nodes as needed.
4. *Backup and Recovery* - Enable snapshotting and backup mechanisms to protect persistent data and allow recovery from failures.


=== SUSE Security

SUSE Security provides runtime container and Kubernetes security, vulnerability scanning, continuous compliance checks, and policy enforcement for applications running on Kubernetes. This section describes how to deploy SUSE Security using the Rancher UI in a reproducible, step-by-step way.

*Preparation*

Before deploying, confirm the following:

* Managed Kubernetes cluster (e.g., RKE2) is registered in SUSE Rancher
* The Rancher server and cluster nodes have network access to pull images (internet or private registry)
* Cluster has sufficient CPU, memory, and network resources for security components
* Kubernetes namespace cattle-neuvector-system will be used for SUSE Security

[NOTE]
If deploying SUSE Security with Rancher Extensions (Rancher Prime), the integration supports single sign-on and deeper UI features; for community editions, Apps & Marketplace or Helm install is typical. 

*Deployment Process*

1. Access Rancher Apps & Marketplace

* Log in to the SUSE Rancher Web UI
* In the left navigation, click Apps & Marketplace (sometimes shown as Apps)

2. Select the SUSE Security Chart

* In Charts, search for:
** neuvector or
** SUSE Security (This corresponds to the Neuvector-based deployment included with Rancher)

* Click the Neuvector/SUSE Security chart to begin installation.

3. Configure Installation Parameters

* Choose the target Kubernetes cluster
* Set the Namespace to:
+
[source,bash]
----
 cattle-neuvector-system
----
* Customize values if needed (defaults are usually suitable for initial deployment)

 Typical settings:
 * Enable runtime security
 * Enable admission control and vulnerability scanning
 * Configure risk thresholds and policy defaults
+
[TIP]
You can enable policy templates such as CIS, PCI-DSS, or GDPR rules as part of the configuration, depending on your compliance needs.

4.  Install the Security Components

* Click Install
* Monitor the deployment until all Neuvector/SUSE Security pods reach Running state

** Go to Cluster → Workloads
** Confirm status of pods under namespace cattle-neuvector-system


*Verification*

Once deployed:

* Access the *SUSE Security dashboard* through Rancher (if UI extension enabled) or by using the exposed service endpoint
* Verify:
** Runtime scanning is active
** Vulnerability and compliance policies are enforced
** Admission control is blocking non-compliant deploys
* Perform a test scan of a sample workload or an image using the policy engine


*Deployment Consideration*

1. *Integration* If using Rancher Prime with the SUSE Security UI extension, you can view security events, alerts, and dashboards directly in Rancher.
2. *Admission Control* To enforce security policies at deploy time, ensure the admission controller is enabled and appropriate policies defined.
3. *Resource Planning* Security scanning and monitoring components consume cluster resources; plan node sizing accordingly.
4. *Compliance Reporting* Use CIS and custom policy templates to generate compliance reports for internal audits.


=== SUSE AI

SUSE AI provides a cloud-native, scalable AI platform for deploying GenAI workloads on Kubernetes. It consists of a collection of modular components (AI middleware, vector databases, inference engines, etc.) that can be installed individually or as a full stack using the SUSE Deployer meta-chart. SUSE AI integrates with SUSE Rancher and conforms to SUSE software entitlement and cluster requirements. 


*Preparation*

Ensure the following prerequisites are completed before deploying SUSE AI:

1. Cluster readiness
* A running Kubernetes cluster (such as SUSE Rancher Prime: RKE2) with all nodes registered in Rancher.
* GPU-enabled Nodes with NVIDIA drivers and CUDA Installed along with Node Labels for AI workloads. (reference - https://documentation.suse.com/suse-ai/1.0/html/AI-requirements/index.html)

2. Entitlement and access
* A valid SUSE AI subscription/entitlement is in place.
* Access to the SUSE Application Collection repository to fetch Helm charts and container images.

3. Namespace
* Create a dedicated Kubernetes namespace for SUSE AI workloads (e.g., suse-ai).

4. Registry credentials
* Create a Kubernetes secret for the SUSE Application Collection registry, using your entitlement credentials.
+
[source,bash]
----
kubectl create namespace suse-ai
kubectl create secret docker-registry application-collection \
  --docker-server=dp.apps.rancher.io \
  --docker-username=<APPCO_USERNAME> \
  --docker-password=<APPCO_USER_TOKEN> \
  -n suse-ai
----

5. Helm registry access
* Log in to the Helm OCI registry holding SUSE AI charts.
+
[source,bash]
----
helm registry login dp.apps.rancher.io/charts \
  -u <APPCO_USERNAME> -p <APPCO_USER_TOKEN>
----

6. cert-manager
* Ensure cert-manager is installed on the cluster (required by certain AI components).


*Deployment Process*

Deploying SUSE AI involves installing each AI component (e.g., Ollama, Milvus, vLLM, Open WebUI, etc.) using the unified SUSE Deployer chart which orchestrates all components and dependencies in one step.

1. Prepare override values
* Create a custom-overrides.yaml file based on one of the {sai-templates}[SUSE-provided templates] (e.g., suse-gen-ai.yaml for production or suse-gen-ai-minimal.yaml for test).

2. Install with Helm
* Use Helm to install the SUSE Deployer chart with your override file:
+
[source,bash]
----
helm upgrade --install suse-ai-deployer \
  --namespace suse-ai \
  --create-namespace \
  --values ./custom-overrides.yaml \
  oci://dp.apps.rancher.io/charts/suse-ai-deployer
----

3. Monitor progress
* Watch the status of AI workloads and pods via kubectl get pods -n suse-ai until all are in a running state.


*Deployment Consideration*

1. *Entitlement and Registry Access* Access to the SUSE Application Collection registry is essential for pulling Helm charts and container images. Ensure credentials are current.
2. *Order of Installation* Install components in dependency order: cert-manager first, then core AI services, then optional components such as PyTorch or GPU-based services.
3. *Resource Planning* AI components (especially vector databases and inference workloads) can be resource intensive. Plan CPU, memory, storage, and GPU capacity accordingly.
4. *Namespace Isolation* Use a dedicated Kubernetes namespace (e.g., suse-ai) to isolate SUSE AI workloads.
5. *Monitoring & Logging* Integrate with SUSE Observability for metrics and logs to monitor performance and troubleshoot issues.


=== ClearML

*add image here*

*Preparation*

1. *HW specification* - For product deployment, the ClearML recommends at least 3 nodes for the control plane, each provisioned with:
  * 8 vCPUs
  * 32 GB RAM
  * 500 GB storage

2. *Software Dependencies*
  * Kubernetes
  * Kubernetes >= 1.23.0-0
  * Kubectl
  * Helm

3. *Networking*
The following network components need to be provisioned to make the ClearML control plane accessible:

* *Ingress Controller* An Ingress controller (e.g., nginx-ingress) is required. If exposing services externally, configure a LoadBalancer-capable solution (e.g. MetalLB).
* *Port specification* The designated nodes for the ClearML control plane servers and workers should be able to communicate through HTTP/S: TCP ports 80 and 443.
Any additional ports to be used in the deployment (i.e.  TCP sessions) should also be pre-configured (see Application Gateway installation).
* *DNS Configuration* A domain with subdomain support is required (referred here-on as <BASE_DOMAIN>), ideally with trusted TLS certificates. All entries must be resolvable by the Ingress controller. Example subdomains:
** Control plane servers:
*** api.<BASE_DOMAIN>
*** app.<BASE_DOMAIN>
*** files.<BASE_DOMAIN>
* Application Gateway:
*** router.<BASE_DOMAIN>
*** tcp-router.<BASE_DOMAIN> (optional, for TCP sessions)
* [For Advanced usage] CNI preconfigured
* [For Multi-tenant deployment] {cml-idp}[IdP] set up per tenant.

4. *Storage*
* A configured StorageClass and an accessible storage backend for the ClearML Control Plane.
* A configured PVC(ReadWriteMany policy)  and PV for the ClearML workers.
5. *ClearML Artifactories*
To install the different ClearML components, credentials to ClearML’s artifact repositories are required, provided by the ClearML team:
* Helm repository credentials (referenced from this point on as <HELM_REPO_TOKEN>)	
* DockerHub registry credentials (referenced from this point on as <CLEARML_DOCKERHUB_TOKEN>)


ClearML’s control plane uses these charts as dependencies

|===
| Name | Version | Repo

| elasticsearch | 8.5.1 | https://helm.elastic.co
| mongodb | 15.6.26 | oci://docker.io/bitnamicharts
| redis | 17.8.3 | oci://docker.io/bitnamicharts
|===



*Deployment process*

1. *Configuring Deployment Parameters*
The installation procedure makes use of a tailored helm chart to deploy the ClearML control plane components.
Deployment specific parameters are set through a custom clearml-values.override.yaml.

2. *Baseline Configuration*
Create a clearml-values.override.yaml file with the baseline ClearML service settings:
Replace the below <BASE_DOMAIN> placeholders with the domain you’ve assigned  to the cluster's Ingress Controller. This will be the base domain for reaching your ClearML installation.
+
[source, bash]
----
imageCredentials:
  password: "<CLEARML_DOCKERHUB_TOKEN>"
clearml:
  cookieDomain: "<BASE_DOMAIN>"
apiserver:
  ingress:
    enabled: true
    hostName: "api.<BASE_DOMAIN>"
  service:
    type: ClusterIP
fileserver:
  ingress:
    enabled: true
    hostName: "files.<BASE_DOMAIN>"
  service:
    type: ClusterIP
webserver:
  ingress:
    enabled: true
    hostName: "app.<BASE_DOMAIN>"
  service:
    type: ClusterIP
clearmlApplications:
  enabled: true
----

3. *Multi-tenant configuration*
+
Establish an initial supervisor tenant with a supervisor user. This user will have the authority to create and remove other tenants, and access information across all tenants.
+
Update the clearml-values.override.yaml with the following values:
Replace the below <SUPERVISOR_COMPANY_ID>, <SUPERVISOR_USER_KEY>, <SUPERVISOR_USER_SECRET> and <SUPERVISOR_USER_EMAIL> with desired values. The <SUPERVISOR_USER_KEY> and <SUPERVISOR_USER_SECRET> can be used to log in as the supervisor user from the ClearML Web UI via app.<BASE_DOMAIN>.
+
Note that the <SUPERVISOR_USER_EMAIL> value must be explicitly quoted (e.g. "\"email@example.com\"").
+
[source, yaml]
----
apiserver:
  extraEnvs:
    - name: CLEARML__services__organization__features__user_management_advanced
      value: "true"
    - name: CLEARML__services__workers__resource_usages__supervisor_company
      value: "<SUPERVISOR_COMPANY_ID>"
    - name: CLEARML__secure__credentials__supervisor__role
      value: "system"
    - name: CLEARML__secure__credentials__supervisor__allow_login
      value: "true"
    - name: CLEARML__secure__credentials__supervisor__user_key
      value: "<SUPERVISOR_USER_KEY>"
    - name: CLEARML__secure__credentials__supervisor__user_secret
      value: "<SUPERVISOR_USER_SECRET>"
    - name: CLEARML__secure__credentials__supervisor__sec_groups
      value: "[\"users\", \"admins\", \"queue_admins\"]"
    - name: CLEARML__secure__credentials__supervisor__email
      value: "\"<SUPERVISOR_USER_EMAIL>\""
    - name: CLEARML__apiserver__company__unique_names
      value: "true"
----

4. *Setting up Helm*
+
Add the ClearML Helm repository to your cluster:
+
[source, bash]
----
helm repo add clearml-enterprise https://raw.githubusercontent.com/clearml/clearml-enterprise-helm-charts/gh-pages --username <HELM_REPO_TOKEN> --password <HELM_REPO_TOKEN>
----
+
Update the local repository:
+
[source, bash]
----
helm repo update
----

5. Deploying the ClearML Control Plane
Install the ClearML Enterprise Control Plane using the configuration in your clearml-values.override.yaml.
+
[source, bash]
----
helm upgrade -i -n clearml clearml clearml-enterprise/clearml-enterprise --create-namespace -f clearml-values.override.yaml
----

6. Creating a Tenant
  * Set up the environment for configuring the server via API:
  The environment variables should match the values defined in the previous installation steps.
+
[source, bash]
----
APISERVER_URL="https://api.<BASE_DOMAIN>"
APISERVER_KEY="<SUPERVISOR_USER_KEY>"
APISERVER_SECRET="<SUPERVISOR_USER_SECRET>"
----
* Create a tenant:
Replace the below <TENANT_NAME> with a name for the new tenant
+
[source, bash]
----
curl $APISERVER_URL/system.create_company \
  -H "Content-Type: application/json" \
  -u $APISERVER_KEY:$APISERVER_SECRET \
  -d '{"name":"<TENANT_NAME>"}'
----
The API returns the new tenant ID (<COMPANY_ID>)
* Validate tenant creation by listing existing tenants:
+
[source, bash]
----
curl -u $APISERVER_KEY:$APISERVER_SECRET $APISERVER_URL/system.get_companies
----
* Create a utility administrator user for to facilitate tenant configuration:
Replace the below <COMPANY_ID> with the value returned when creating the tenant.
Replace the below <ADMIN_USER_NAME> and <ADMIN_USER_EMAIL> with suitable values.
+
[source, bash]
----
curl $APISERVER_URL/auth.create_user \
  -H "Content-Type: application/json" \
  -u $APISERVER_KEY:$APISERVER_SECRET \
  -d '{"name":"<ADMIN_USER_NAME>","company":"<COMPANY_ID>", \
     "email":"<ADMIN_USER_EMAIL>","role":"admin","internal":"true"}'
----
* Create a utility API credentials for this administrator user:
+
[source, bash]
----
curl $APISERVER_URL/auth.create_credentials \
  -H "Content-Type: application/json" \
  -H "X-Clearml-Impersonate-As: <USER_ID>" \
  -u $APISERVER_KEY:$APISERVER_SECRET
----
The returned set of key and secret can be used for additional API configuration of the tenant.
* Onboarding Users through an Identity Provider (SSO)
ClearML supports a wide range of protocols and providers. In ClearML, each tenant is associated with its own IdP to authenticate and authorize access to the ClearML platform.
Refer to ClearML’s {cml-idp}[online documentation] for further information on configuring each IdP
* Verifying the installation of Control Plane
To verify the ClearML control plane is up and running use a web browser to access (https://app.<BASE_DOMAIN>;).

*image required*
* Setting up Worker Nodes
The ClearML Agent is installed on Kubernetes using a Helm chart. This sets up a controller pod that listens to ClearML queues and launches jobs as needed. The Agent can be deployed on the same Kubernetes cluster as the ClearML Control Plane or on a separate one.
The Controller pod:
* Pulls jobs from ClearML execution queues
* Prepares a Kubernetes job based on the provided YAML template.
* Inside each job pod, the ClearML Agent:
** Installs the required environment for the task.
** Executes and monitors the task process.
** Logs task data to the ClearML Server

*Prerequisites*
In a multi-tenant deployment, tenant isolation is achieved by creating a dedicated Kubernetes namespace  for each tenant, and deploying a dedicated ClearML agent for each tenant (using a tenant admin’s API credentials).
* A running ClearML Control plane
* Each worker environment must be able to access the ClearML Server over the same network.






=== Software

// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// As with the Compute platform, provide step-by-step guidance for
// preparing and deploying the Software.
// Solutions almost always have multiple Software components deployed
// as layers.
// So, use of subsections is likely.
// The heading for each subsection should reflect the layer (product)
// being deployed; for example, ‘SUSE Rancher Prime deployment’.
// Remember to use the defined variables for product names,
// websites, etc.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

The following sections provide deployment guidance for each software layer.


==== Software product deployment


*Preparation*
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide step-by-step guidance for preparing the software component.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

Add content here


*Process*
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide step-by-step guidance for preparing the softwware component.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

Add content here


*Considerations*
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Note any considerations for deployment not covered above, such as automation, storage expansion, future scaling, etc.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

Add content here




== Validation
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide validation steps for testing that the solution deployment
// was successful and configured properly.
// This could entail a list of specific checks (e.g., commands and
// expected output), running an automated tool for performing these
// checks, or implementing a functional demonstration.
// Include commands, output, and screenshots, as needed.
// Remember to represent steps as an ordered list.
// Use subsections if needed for clarity.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

Add content here




== Summary
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Summarize:
// - Solution description
// - Motivation for the solution
// - What was covered in this document
// - Suggested next steps for the learning journey
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

Add content here



== Frequently Asked Questions (FAQs)
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// If possible provide an unordered list of frequently asked questions
// with the respective answers.
// Answers are contained in open blocks associated with each question.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

. Question 1?
+
--
Answer paragraph 1.
Answer paragraph 2.
Image
--



== References
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide a list of the references used in this document.
// Use variables to simplify future updates, such as:
// * {ref-url}[Reference title]
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =


* Reference 1
* Reference 2



== Glossary
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Provide a glossary of key terms used in this document.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

Term::
Definition

Term::
Definition




== Appendix
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Use subsections of the Appendix to provide such content as:
// * a complete bill of materials for all components
// * benchmark results
// * other supporting content
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

The following sections provide additional information and resources.


=== Compute platform bill of materials

Add content here


=== Software bill of materials

Add content here


=== Benchmark results

Add content here




// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
// Do not modify below this break.
// = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =

++++
<?pdfpagebreak?>
++++


:leveloffset: 0

== Legal notice
include::common_trd_legal_notice.adoc[]

++++
<?pdfpagebreak?>
++++


:leveloffset: 0
include::common_gfdl1.2_i.adoc[]

//end
